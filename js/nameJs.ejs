<script>
    var socket = io();

    // 보내기 버튼 클릭했을 때 채팅 입력
    // $('#nameSendBtn').click(function () {
    //     socket.emit('user join', $('#nameInput').val());
    //     $('#nameInput').val('');
    //     return false;
    // });

    // 엔터치면 채팅 입력
    // $('#nameInput').keydown(function (key) {
    //     if (key.keyCode == 13) {
    //         socket.emit('user join', $('#nameInput').val());
    //         $('#nameInput').val('');
    //         return false;
    //     }
    // });

    // socket.on('guestIn', function (data) {
    //     $('#messages').append($('<li id="noti">').text(data + ' 님이 입장하셨습니다.'));
    //     $('#chatLog').scrollTop($('#chatLog')[0].scrollHeight);
    // });

</script>